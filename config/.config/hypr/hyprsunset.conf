# Redshift-style blue-light filtering for Wayland. A single midnight profile
# keeps the filter enabled all day instead of switching back to identity mode.
profile {
    time = 00:00
    temperature = 4500
    gamma = 1.0
}
