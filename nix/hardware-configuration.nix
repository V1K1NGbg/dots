{ ... }:

{
  # install.sh replaces this tracked placeholder with the target machine's
  # generated hardware configuration. Keeping the path tracked ensures Nix
  # includes the generated replacement when evaluating this Git checkout.
}
