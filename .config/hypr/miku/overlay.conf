breeding=true
mascot_limit=5
dragging=true
ie_interactions=false
ie_throwing=false
per_mascot_interactions=false
mascot_scale=0.75
# Interpolation makes the visible falling sprite diverge from its click target.
interpolation_framerate=0
# Overlay (3) stays above the audio visualizer's bottom layer.
overlay_layer=3
